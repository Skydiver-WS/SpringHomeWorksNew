version: '3'
services:
  postgres:
    image: postgres:latest
    environment:
      - POSTGRES_DB=list_students
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - '5432:5432'
    volumes:
      - ./init.sql:/docker-entrypoint-init-db/init.sql
